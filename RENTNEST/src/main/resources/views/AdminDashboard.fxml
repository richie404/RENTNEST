<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.rentnest.controllers.AdminController"
            stylesheets="@../css/admindashboard.css">

    <!-- HEADER -->
    <top>
        <HBox styleClass="header">
            <Label text="⚙ RentNest Admin Dashboard" styleClass="header-title"/>
            <Button text="Logout" onAction="#handleLogout" styleClass="button-logout"/>
        </HBox>
    </top>

    <!-- MAIN CONTENT -->
    <center>
        <VBox alignment="CENTER" spacing="20" styleClass="content">

            <Label text="User Management" styleClass="section-title"/>
            <HBox spacing="15">
                <Button text="Approve Owners" styleClass="button-primary"/>
                <Button text="Manage Renters" styleClass="button-primary"/>
            </HBox>

            <Label text="Listings" styleClass="section-title"/>
            <HBox spacing="15">
                <Button text="Approve Listings" styleClass="button-primary"/>
                <Button text="Flagged Listings" styleClass="button-primary"/>
            </HBox>

            <Label text="Reports & Analytics" styleClass="section-title"/>
            <HBox spacing="15">
                <Button text="Revenue Reports" styleClass="button-primary"/>
                <Button text="User Activity Logs" styleClass="button-primary"/>
            </HBox>
        </VBox>
    </center>

</BorderPane>
